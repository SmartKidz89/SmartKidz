# Copy to .env.local for local dev, and set the same values in Vercel Project Settings.

# --- Core URLs ---
# If deploying marketing + app on the same Vercel domain, leave NEXT_PUBLIC_APP_ORIGIN empty.
NEXT_PUBLIC_APP_ORIGIN=
NEXT_PUBLIC_APP_URL=http://localhost:3000

# --- Supabase (public) ---
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=

# --- Supabase (server) ---
SUPABASE_URL=
SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE_KEY=

# --- Stripe ---
STRIPE_SECRET_KEY=
STRIPE_WEBHOOK_SECRET=
STRIPE_PRICE_MONTHLY=
STRIPE_PRICE_ANNUAL=

# --- Email (optional) ---
RESEND_API_KEY=
FROM_EMAIL=

# --- Admin tools (optional) ---
ADMIN_IMPORT_TOKEN=
ADMIN_EXPORT_TOKEN=

# --- Cron / reports (optional) ---
CRON_SECRET=
WEEKLY_REPORT_SECRET=

# --- OpenAI (optional tools) ---
OPENAI_API_KEY=
OPENAI_MODEL=gpt-4o-mini

# --- Preview behavior (Vercel) ---
# marketing | app
PREVIEW_AS=marketing

# --- Hosts (optional; advanced) ---
APP_HOSTS=
APP_HOST_SUFFIX=


# --- Product Analytics + Feature Flags (optional; recommended) ---
# Used for event tracking, session insights, and remote config/experiments.
NEXT_PUBLIC_POSTHOG_KEY=
NEXT_PUBLIC_POSTHOG_HOST=https://app.posthog.com

# --- Error monitoring (optional; recommended) ---
SENTRY_DSN=
NEXT_PUBLIC_SENTRY_DSN=
SENTRY_AUTH_TOKEN=

# --- Mascot animation (optional) ---
NEXT_PUBLIC_RIVE_MASCOT_URL=

# Demo mode (deterministic UAT without Supabase)
NEXT_PUBLIC_DEMO_MODE=0

# Admin: generate missing assets (Option B)
ADMIN_GENERATE_ASSETS_TOKEN=
OPENAI_IMAGE_MODEL=gpt-image-1
OPENAI_IMAGE_SIZE=1024x1024
SUPABASE_ASSETS_BUCKET=assets

# --- Stable Diffusion (Forge / A1111 SDAPI) ---
# If set, the admin generator can use Forge locally instead of OpenAI.
# For Docker, use http://host.docker.internal:7860
IMAGE_PROVIDER=forge
SD_API_URL=http://127.0.0.1:7860
