"use client";

/**
 * Native scrolling wrapper.
 * Previously used Lenis for smooth scrolling, but native is more performant and accessible.
 */
export default function SmoothScroll({ children }) {
  return children;
}